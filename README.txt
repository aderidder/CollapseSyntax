An OpenClinica Export can contain data from multiple versions of a single case report
form. These multiple versions each generate a complete set of columns, which can be
extremely bothersome for data analysis. This program attempts to collapse the columns.

The program requires (at least) java 1.8